// Shared library of reusable helpers for Advent of Code solutions.
//
// Intentionally starts empty. Add a function here once you've needed the
// same piece of logic in two or more days — don't pre-build a grid/parsing
// toolkit speculatively. Split this into topic files (lib/grid.ts,
// lib/parse.ts, ...) once it earns that structure.
//
// Import it from any day via the `@lib` path alias, e.g.:
//   import { something } from "@lib";
export {};
